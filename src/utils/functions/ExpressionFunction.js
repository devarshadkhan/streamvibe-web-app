/**
 * Arshad Khan
 * @param {This Function is create is a First Capital Letter} value 
 * @returns  string 
 */
export function capitalizeFirstLetter(value) {
    return value.charAt(0).toUpperCase() + value.slice(1);
}